machine:
  ruby:
    version: 2.1.3
dependencies:
  pre:
    - sudo apt-get install rpm
test:
  override:
    - bundle exec jekyll build
    - utils/create_rpm.sh
  post:
    - cp _dist/* $CIRCLE_ARTIFACTS/

